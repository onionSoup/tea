<div id="new_time_limit_flex">
  <%= render 'layouts/admin_side_nav' %>
  <div>
    <h2>注文募集期間の設定</h2>
    <div id ="new_time_limit_field">
      <p>esmの皆様が希望するお茶の入力ができる期間を設定します。</p>
      <%= form_for(@time_limit) do |f| %>
        <%= f.label :start, '開始日時:'%>
        <%= f.date_select :start, :use_month_numbers => true, :default => Date.today - 1  %>
        <br />
        <%= f.label :end, '終了日時:' %>
        <%= f.date_select :end, :use_month_numbers => true, :default => Date.today >> 1 %>
        <%= f.submit '設定する' %>
      <% end %>
    </div>
  </div>
</div>
