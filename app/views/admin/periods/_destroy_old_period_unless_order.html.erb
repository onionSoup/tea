<p id="can_not_set_period_p">期限の設定はできません。注文期間を削除後に期限を選択することができるようになります。</p>
<%= form_tag(admin_period_path, method: :put) do %>
    <!-- start_yeatの欄に１年前があるのは、１年後の期限を誤操作で指定した時のやり直し用。-->
    <p id="period_states_form_p">注文期限:
      <%= select_date @end_time,      {disabled: true} %>
      <%= submit_tag '注文期限の設定', {disabled: true ,data: 'select_date'} %>
    </p>
<% end %>

<% if Period.can_destroy? %>
  <p>注文期間を削除・再設定して、ユーザーがお茶を注文できるようにするために<br>
  以下のボタンを押してください。</p>
  <p>注文期間を削除・再設定することで新規注文を受け付けることができるようになります。<p>
  <%= button_to '注文期間を削除する', {action: 'destroy'}, {disabled: false, method: :delete} %>
<% else %>
  <p>期間の削除はできません。注文期間中に、ユーザーがお茶を注文しています。<br>
  すべての注文を引換し<%= link_to '引換情報を削除', exchanged_path %>した後、ボタンを押せます。</p>
  <p>注文期間を削除・再設定することで新規注文を受け付けることができるようになります。<p>
  <%= button_to '注文期間を削除する', {action: 'destroy'}, {disabled: true,  method: :delete, data: 'delete'} %>
<% end %>
