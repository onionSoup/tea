<h1>Listing admin_orders</h1>

<table>
  <thead>
    <tr>
      <th>Time limit</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @admin_orders.each do |admin_order| %>
      <tr>
        <td><%= admin_order.time_limit_id %></td>
        <td><%= link_to 'Show', admin_order %></td>
        <td><%= link_to 'Destroy', admin_order, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>
<br>