<h2><%= h2_title_of_edit_order %></h2>
<div id="edit_order_content">
  <h3 id='add_order_in_edit_order'>注文の追加</h3>
  <p>注文したい商品と個数を選んで、「注文する」ボタンを押してください</p>
  <%=  form_tag order_path, method: :put do  %>
    <%= label_tag 'item_id', '品名：' %>
    <%= select_tag(
      'order[order_details_attributes][0][item_id]',
       options_for_select(item_options)
    ) %>
    <%= label_tag 'item_id', '数量：' %>
    <%= select_tag(
      'order[order_details_attributes][0][quantity]',
      options_for_select(quantity_options)
    ) %>
    <%= button_tag :注文する %>
  <% end %>

  <h3 id='ordered_index_in_edit_order'><%= current_user.name %>さんの注文一覧</h3>
  <% if @order.order_details.any? %>
  <p><%= current_user.name %>さんは、以下のお茶を注文しています。</p>
  <p>注文をキャンセルしたいお茶がある場合、「削除する」リンクを押してください。</p>
    <table id="edit_order_table">
      <tr>
        <th class="name">品名</th>
        <th class="quantity">数量</th>
        <th class="price">単価</th>
        <th class="sum">小計</th>
        <th></th>
      </tr>
      <% @order.order_details.each do |detail| %>
          <tr>
            <td class="name"><%= detail.item.name %></td>
            <td class="quantity"><%= detail.quantity %>個</td>
            <td class="price"><%= detail.then_price %>円</td>
            <td class="sum"><%= detail.quantity * detail.then_price %>円</td>
            <td><%= link_to '削除する','#'%></td>
          </tr>
        <% end %>
    </table>
  <% else %>
    <p><%= current_user.name %>さんは、まだお茶を注文していません。注文すると、ここにお茶が表示されます。</p>
  <% end %>
</div>


